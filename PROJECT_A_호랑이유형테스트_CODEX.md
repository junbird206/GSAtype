# PROJECT A — 고려대 2학기 생존 호랑이 유형 테스트

> 이 문서를 Codex에 통째로 전달하고 시작할 것.

---

## 0. 프로젝트 개요

고려대학교 재학생을 대상으로 한 심리테스트 웹페이지. 8개 질문에 답하면 6가지 "호랑이 유형" 중 하나가 나오고, 결과를 이미지로 저장해 인스타그램 스토리에 공유할 수 있다. 결과 페이지에서 Google의 학생 대상 AI 요금제 무료 혜택 가입 링크로 연결한다.

**목적 우선순위**
1. 자발적 공유(인스타 스토리)를 통한 확산
2. 결과 페이지에서의 가입 링크 클릭
3. 체류시간이나 완주율은 부차적

**타겟**: 고려대학교 학부생 (2학기 개강 직전~개강 직후)

---

## 1. 기술 요구사항 (엄수)

- **정적 사이트. 백엔드/DB 없음.** 모든 로직은 클라이언트에서 처리
- **배포: Cloudflare Pages** (무제한 대역폭, 무료). 빌드 산출물이 정적 파일이어야 함
- **모바일 퍼스트.** 트래픽의 95% 이상이 모바일. 데스크톱은 중앙 정렬로 대응만
- 프레임워크는 자유롭게 선택하되 **번들 크기를 최소화**할 것. 바이럴 시 초기 로딩이 이탈을 결정함
- 외부 이미지 CDN 의존 금지. 에셋은 리포지토리에 포함
- **질문/유형/결과 데이터는 코드에서 분리된 별도 데이터 파일**로 관리할 것 (콘텐츠 수정이 잦음)
- Google Analytics 4 삽입 (측정 ID는 placeholder로 두고 주석 표기)

---

## 2. 사용자 플로우

```
[랜딩] → [질문 1~8] → [결과 분석 중 (3초 연출)] → [결과 페이지] → [이미지 저장 / 공유 / 가입 링크]
```

### 2-1. 랜딩

- 타이틀: `나의 2학기 생존 호랑이 유형은?`
- 서브: `8문항, 30초면 끝`
- 시작 버튼
- 하단에 `지금까지 N명이 참여했어요` 같은 카운터는 **넣지 말 것** (백엔드가 없어 거짓말이 됨)

### 2-2. 질문 화면

- **한 화면에 한 질문.** 스크롤 없이 보여야 함
- 상단에 진행률 바 (1/8)
- 선택지를 탭하면 즉시 다음 질문으로 자동 이동 (별도 "다음" 버튼 없음)
- 뒤로 가기 버튼 제공
- 전환 애니메이션은 200ms 이내로 짧게

### 2-3. 결과 분석 중 (3초)

- "당신의 호랑이를 찾는 중..." 로딩 연출
- **정확히 3초.** 스킵 버튼 없음
- 이 화면 하단에 가입 안내 배너를 배치한다 (아래 6번 문구 규칙 참조)
- 로딩 인디케이터는 실제 진행되는 것처럼 보이게 (게이지가 차오르는 형태)

### 2-4. 결과 페이지

위에서부터 순서대로:

1. 유형 캐릭터 이미지 + 유형명
2. 한 줄 요약
3. 특징 설명 (3~4문장)
4. `2학기 궁합` — 잘 맞는 유형 / 안 맞는 유형 각 1개
5. `이 호랑이를 위한 Gemini 치트키` — 유형별 프롬프트 1개 (복사 버튼 포함)
6. **결과 이미지 저장 버튼** (가장 눈에 띄게)
7. 친구에게 공유 버튼 (링크 복사)
8. **가입 CTA** (아래 6번 참조)
9. 테스트 다시 하기

**중요**: 6번 이미지 저장 버튼이 이 페이지의 1순위 행동이다. 8번 가입 CTA가 2순위. 이 위계가 시각적으로 드러나야 한다.

---

## 3. 유형 정의 (6종)

| ID | 유형명 | 특징 | Gemini 활용 스타일 |
|---|---|---|---|
| `white` | 중앙광장 지하 거주 백호랑이 | 1학기 학점 충격 후 2학기엔 중광·하스에서 밤샘을 다짐한 학점 복구형 | NotebookLM으로 전공서적 수백 페이지를 3분 요약해 뇌에 주입 |
| `cheer` | 고연전 뽕 가득 응원단 호랑이 | 9월 고연전과 뒤풀이, 동아리에 온 영혼을 바치는 인싸형 | 팀플 회의록 요약과 발표 대본을 맡기고 응원 연습하러 감 |
| `eng` | 하나스퀘어 밤샘 공대 붉은 호랑이 | 과제·실험·코딩 에러 박살내기에 갇힌 이공계 전사 | 에러 로그와 수식을 던져서 1초 해독 |
| `void` | 참살이길 카페 난민 우주공강 호랑이 | 수강신청 망해서 참살이길을 떠돌며 멍때리는 유형 | 딴짓용. "교수님 헌정 랩 가사 지어줘" 하며 시간 때움 |
| `spec` | 정후·법후 고시방 스펙업 호랑이 | 학회·소모임·공모전 스펙 쌓기에 일찍 눈뜬 조기 열정형 | 기획서 초안과 자소서를 순식간에 구조화 |
| `romance` | 안암골 막걸리 사발 낭만파 호랑이 | 공부보다 동기들과의 낭만과 술자리가 최고인 유유자적형 | 숙취로 결석할 때 정중한 메일을 대신 써주는 용도 |

각 유형마다 필요한 데이터: `id`, `name`, `emoji`, `oneLiner`, `description`, `geminiStyle`, `goodMatch`, `badMatch`, `prompt`, `imageAsset`

---

## 4. 질문 및 채점 로직

### 채점 방식

- 각 선택지는 특정 유형에 **+2점**, 보조 유형에 **+1점**을 부여
- 8문항 완료 후 총점이 가장 높은 유형이 결과
- **동점 시**: 앞선 문항에서 더 높은 점수를 받은 유형이 우선. 그래도 동점이면 배열 순서상 먼저인 유형
- 채점 테이블은 데이터 파일에 분리

### 질문 8개

**Q1. 2학기 첫 전공 팀플, 조장이 안 정해졌을 때 나는?**
- A) 답답해서 내가 조장 맡고 기획서 초안부터 뽑는다 → `spec`+2, `white`+1
- B) 서기나 발표 맡고 편하게 가는 법을 연구한다 → `void`+2, `cheer`+1
- C) 일단 조용히 있는다. 누가 하겠지 → `romance`+2, `void`+1
- D) 자료조사랑 분석 파트를 자원한다 → `eng`+2, `white`+1

**Q2. 수강신청 망해서 우주공강 4시간이 생겼다.**
- A) 중광 지하 가서 노트북 켠다 → `white`+2, `eng`+1
- B) 참살이길 가서 동기 불러낸다 → `romance`+2, `cheer`+1
- C) 카페에서 혼자 멍때리다 하루 끝난다 → `void`+2, `romance`+1
- D) 그 시간에 공모전 서류 쓴다 → `spec`+2, `white`+1

**Q3. 1학기 학점표를 받았을 때 내 심정은?**
- A) 2학기엔 싹 쓸어담는다. 복수혈전 → `white`+2, `spec`+1
- B) 학점이 무슨 상관, 9월 고연전이나 즐기자 → `cheer`+2, `romance`+1
- C) 전공만 살렸으면 됐다 → `eng`+2, `void`+1
- D) 안 봤다. 안 볼 거다 → `void`+2, `romance`+1

**Q4. 영문 논문 50페이지 과제가 나왔다.**
- A) PDF 던져서 3줄 요약부터 시킨다 → `white`+2, `spec`+1
- B) 커피 사들고 한 줄씩 번역하며 벼락치기 → `eng`+2, `white`+1
- C) 마감 전날까지 안 연다 → `void`+2, `romance`+1
- D) 조원들이랑 나눠서 하자고 제안한다 → `cheer`+2, `spec`+1

**Q5. 금요일 밤, 내 기본값은?**
- A) 안암 어딘가에서 술자리 → `romance`+2, `cheer`+1
- B) 동아리 또는 학회 모임 → `cheer`+2, `spec`+1
- C) 집. 밀린 과제 → `eng`+2, `white`+1
- D) 도서관. 주말에 놀려고 미리 함 → `white`+2, `spec`+1

**Q6. 카톡 프로필이 지금 어떤 상태인가?**
- A) 단체 사진, 최근 행사 → `cheer`+2, `romance`+1
- B) 몇 년째 그대로 → `eng`+2, `void`+1
- C) 다이어리나 목표 문구 → `spec`+2, `white`+1
- D) 아무것도 없음 → `void`+2, `eng`+1

**Q7. 9월 고연전, 나는?**
- A) 응원 연습부터 뒤풀이까지 풀참 → `cheer`+2, `romance`+1
- B) 경기만 보고 조용히 귀가 → `white`+2, `eng`+1
- C) 뒤풀이만 간다 → `romance`+2, `cheer`+1
- D) 그날 과제 마감이라 못 간다 → `eng`+2, `spec`+1

**Q8. 2학기가 끝났을 때 가장 듣고 싶은 말은?**
- A) "너 학점 미쳤더라" → `white`+2, `spec`+1
- B) "너 진짜 재밌게 산다" → `romance`+2, `cheer`+1
- C) "그거 네가 만든 거야?" → `eng`+2, `spec`+1
- D) "너 요즘 뭐 그렇게 바빠?" → `spec`+2, `cheer`+1

---

## 5. 결과 이미지 생성 (핵심 기능)

**이게 확산의 전부다. 여기 가장 공들일 것.**

- **Canvas API로 클라이언트에서 JPG/PNG 생성.** 외부 라이브러리 사용 시 번들 크기 확인
- **사이즈: 1080 × 1920 (인스타 스토리 비율)**
- 이미지 구성:
  - 유형 캐릭터 일러스트
  - 유형명 (크게)
  - 한 줄 요약
  - 하단에 **테스트 URL을 크고 명확하게** — 스토리 이미지 안의 텍스트는 클릭이 안 되므로 외워서 칠 수 있어야 함
- 저장 버튼을 누르면 모바일에서 바로 사진첩에 저장되거나 공유 시트가 열려야 함 (`navigator.share` API 우선, 미지원 시 다운로드 폴백)
- 저장 직후 안내 문구 노출: `스토리에 올릴 때 링크 스티커도 같이 달아주세요!`

---

## 6. 문구 규칙 (규정 사항 — 반드시 지킬 것)

### 제품명

- 정확한 명칭: **Google AI Plus** / 학생 혜택 공식명 **Google AI Plan for Students**
- **"Gemini Plus", "Gemini Pro 요금제", "제미나이 프로" 등은 사용 금지** (존재하지 않는 명칭이거나 다른 제품임)
- Gemini는 제품(모델/앱) 이름으로만 사용

### 대상 표기

- 혜택 대상은 **대학생·대학원생 전체**임. "고려대 학생만"은 사실과 다르므로 금지
- 권장 문구: `대학생·대학원생이라면 Google AI Plus 12개월 무료`

### 금지 사항

- Google / Gemini 로고를 임의로 사용하지 말 것. 텍스트로만 표기
- 금액 환산 표현 금지 (`월 OO원 상당` 등)
- 고액 경품·추첨 유도 문구 금지

### 필수 요소

- 가입 링크는 `TEAM_SIGNUP_LINK` 상수로 빼두고 placeholder 처리 (실제 링크는 나중에 주입)
- 결과 페이지 하단 문구:
  > 이 테스트, Gemini로 만들었습니다.
  > 대학생·대학원생은 Google AI Plus 12개월 무료 →

---

## 7. 디자인 방향

- 고려대 크림슨 계열을 기본 톤으로. 다만 학교 공식 로고·엠블럼은 사용하지 말 것
- 호랑이 캐릭터는 **일러스트 스타일 통일**이 중요. 6종이 같은 화풍이어야 함
- 캐릭터 에셋은 초기엔 placeholder(단색 실루엣 + 이모지)로 두고, 구조를 먼저 완성할 것. 실제 일러스트는 나중에 교체
- 폰트는 웹폰트 1개까지만. 로딩 지연 주의

---

## 8. 개발 순서

1. 데이터 구조 + 채점 로직 (테스트 코드 포함)
2. 질문 화면 → 결과 화면까지의 플로우
3. 결과 이미지 생성 (가장 중요)
4. 공유·저장 UX
5. 디자인 마감
6. Cloudflare Pages 배포 설정

**1~3단계가 끝나면 일단 배포 가능한 상태를 만들 것.** 디자인 완성도보다 동작하는 링크가 먼저 필요하다.

---

## 9. 하지 말아야 할 것

- 백엔드, DB, 로그인 붙이지 말 것
- 사용자 개인정보(이름, 학번, 이메일) 수집하지 말 것
- 결과를 서버에 저장하지 말 것
- 참여자 수 카운터처럼 서버가 필요한 기능 넣지 말 것
- 무거운 애니메이션 라이브러리 도입 금지
